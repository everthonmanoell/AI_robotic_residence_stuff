import pprint
import sys

import flask
import pandas
import requests

print(requests.__version__) #requests é um pacote instalado via pip que serve para fazer requisições HTTP e o .__version__ mostra a versão instalada
# print('Python:', sys.executable)
# print('Prefixo:', sys.prefix)
# pprint.pp(sys.path[:3])

